# === Configuración de Base de Datos (DEV) ===
spring.datasource.url=jdbc:postgresql://localhost:5432/paymentdb_dev
spring.datasource.username=postgres
spring.datasource.password=admin
spring.datasource.driver-class-name=org.postgresql.Driver

# === Hibernate / JPA ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# === PayU Sandbox Configuration (solo para DEV) ===
payu.account.id=512321
payu.merchant.id=508029
payu.api.login=pRRXKOl8ikMmt9u
payu.api.key=4Vj8eK4rloUd272L48hsrarnUA
payu.response.url=https://dev-tunel.ngrok.app/webhook/payu/notification
payu.api.url=https://sandbox.api.payulatam.com/payments-api/4.0/service.cgi

# === Swagger (solo dev)
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.default-produces-media-type=application/json
